# Sprint 31 — P2-Schulden (#144, #145, #146) Implementierungsplan

> **Für agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Drei P2-Issues schließen: Canvas-Kontext-Bugs verifizieren, Sharing-Fix bestätigen, Tier-Gating konsolidieren.

**Architecture:** Bugs in #144 und #145 wurden bereits in früheren Commits behoben. Sprint 31 verifiziert das, schließt die Issues und führt die verbleibende #146-Arbeit durch (2 Routen auf requireFeature() migrieren + billing_portal-Key).

**Tech Stack:** Next.js 16 App Router, TypeScript strict, Supabase, requireFeature() aus lib/utils/tier-check.ts, FEATURE_TIERS aus config/tiers.ts

---

### Erkenntnisse aus Code-Analyse (vor Implementierung)

- **#144** (`canvas-context.ts`): `matchesTerm()`, Finance-Guard und PREFILL_FIELDS-Denominator-Fix sind alle live. `src/__tests__/unit/canvas-context.test.ts` hat 28 Tests inkl. Gold-Canvas-Suite — alle grün. → Nur Issue schließen.
- **#145** (Share API + View): `CreateShareSchema` nutzt `z.enum(SHARING_MODULES)` mit 6 Modulen. `/share/[token]/page.tsx` rendert alle 6. → Nur Issue schließen.
- **#146**: `requiresTier()` existiert nicht im Codebase. `export/pdf/route.ts` hat dual-check (manuell + requireFeature). `settings/portal/route.ts` hat manuellen Tier-Check. `usecase/route.ts` ist ein Mengen-Limit (kein Feature-Gate) → unverändert lassen.

---

### Task 1: billing_portal-Key zu FEATURE_TIERS hinzufügen

**Files:**
- Modify: `src/config/tiers.ts`

- [ ] In `FEATURE_TIERS` nach `presentation_templates: 'pro'` einfügen:
  ```ts
  billing_portal: 'pro',
  ```
- [ ] TypeScript prüfen: `npx tsc --noEmit`
  Erwartet: keine Fehler
- [ ] Tests laufen lassen: `npx jest src/__tests__/unit/tier-check.test.ts --no-coverage`
  Erwartet: alle bestehenden Tests grün (tabellengetriebener Test deckt billing_portal automatisch ab)

---

### Task 2: export/pdf/route.ts — manuellen Basis-Check ersetzen

**Files:**
- Modify: `src/app/api/export/pdf/route.ts`

**Vorher** (Zeilen 47–64 ca.):
```ts
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) { return NextResponse.json({ error: 'Nicht angemeldet' }, { status: 401 }) }

const { data: profileData } = await supabase
  .from('profiles')
  .select('tier, company')
  .eq('id', user.id)
  .single() as { data: { tier: string; company: string | null } | null }

const tier = profileData?.tier ?? 'free'
if (tier === 'free') {
  return NextResponse.json(
    { error: 'PDF-Export ist ein Pro-Feature.', code: 'UPGRADE_REQUIRED' },
    { status: 403 }
  )
}
```

**Nachher:**
```ts
const gate = await requireFeature('pdf_export')
if (gate instanceof NextResponse) return gate
const { userId } = gate

const supabase = await createClient()
const { data: profileData } = await supabase
  .from('profiles')
  .select('company')
  .eq('id', userId)
  .single() as { data: { company: string | null } | null }
```

- [ ] Zeilen ersetzen wie oben beschrieben
- [ ] Sicherstellen dass `userId` (aus gate) für alle folgenden Supabase-Queries `.eq('user_id', userId)` korrekt gesetzt ist
- [ ] `const { data: { user } }` und manuelle `user.id`-Referenzen durch `userId` aus gate ersetzen
- [ ] `npx tsc --noEmit` — keine Fehler erwartet
- [ ] `npx jest src/__tests__/security/ --no-coverage` — alle Security-Tests grün

---

### Task 3: settings/portal/route.ts — manuellen Tier-Check ersetzen

**Files:**
- Modify: `src/app/api/settings/portal/route.ts`

**Vorher:**
```ts
export async function POST() {
  try {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

    const { data: profile } = await supabase
      .from('profiles')
      .select('tier, stripe_customer_id')
      .eq('id', user.id)
      .single() as { data: { tier: string; stripe_customer_id: string | null } | null }

    if (!profile || profile.tier === 'free') {
      return NextResponse.json({ error: 'Nur für Pro/Enterprise verfügbar' }, { status: 403 })
    }
    if (!profile.stripe_customer_id) {
      return NextResponse.json({ error: 'Kein Stripe-Kunde gefunden' }, { status: 400 })
    }
```

**Nachher:**
```ts
export async function POST() {
  try {
    const gate = await requireFeature('billing_portal')
    if (gate instanceof NextResponse) return gate
    const { userId } = gate

    const supabase = await createClient()
    const { data: profile } = await supabase
      .from('profiles')
      .select('stripe_customer_id')
      .eq('id', userId)
      .single() as { data: { stripe_customer_id: string | null } | null }

    if (!profile?.stripe_customer_id) {
      return NextResponse.json({ error: 'Kein Stripe-Kunde gefunden' }, { status: 400 })
    }
```

- [ ] Import hinzufügen: `import { requireFeature } from '@/lib/utils/tier-check'`
- [ ] Code wie oben ersetzen
- [ ] `npx tsc --noEmit` — keine Fehler erwartet

---

### Task 4: Vollständigen Test-Run + Commit

- [ ] `npx jest --no-coverage 2>&1 | tail -10`
  Erwartet: alle Test Suites grün
- [ ] Git-Status prüfen: `git diff --stat`
- [ ] Commit:
  ```bash
  git add src/config/tiers.ts src/app/api/export/pdf/route.ts src/app/api/settings/portal/route.ts
  git commit -m "feat(#146): Tier-Gating konsolidieren — billing_portal-Key + 2 Routen auf requireFeature() migriert"
  ```

---

### Task 5: GitHub-Issues schließen

- [ ] `gh issue close 144 --comment "Alle drei Bugs wurden in früheren Commits behoben. matchesTerm() seit Sprint-N, Finance-Guard und PREFILL_FIELDS-Denominator-Fix seit Sprint 30. Gold-Canvas-Suite (28 Tests) grün."`
- [ ] `gh issue close 145 --comment "Share-API unterstützt seit Sprint 21 6 Module via z.enum(SHARING_MODULES). Share-View-Page rendert alle 6 Module. Keine toten Links."`
- [ ] `gh issue close 146 --comment "requiresTier() existiert nicht im Codebase. billing_portal: 'pro' in FEATURE_TIERS ergänzt. export/pdf + settings/portal auf requireFeature() migriert. usecase/route.ts bleibt als Mengen-Limit unverändert (korrekt)."`

---

### Task 6: Push + Obsidian

- [ ] `git push origin main`
- [ ] Obsidian Sprint-Log aktualisieren
